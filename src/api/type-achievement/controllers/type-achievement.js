'use strict';

/**
 * type-achievement controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::type-achievement.type-achievement');
