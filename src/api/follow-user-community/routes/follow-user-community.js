'use strict';

/**
 * follow-user-community router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::follow-user-community.follow-user-community');
