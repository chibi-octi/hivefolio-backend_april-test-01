'use strict';

/**
 * follow-user-community service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::follow-user-community.follow-user-community');
