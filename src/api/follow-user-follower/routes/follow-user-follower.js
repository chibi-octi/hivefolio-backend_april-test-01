'use strict';

/**
 * follow-user-follower router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::follow-user-follower.follow-user-follower');
