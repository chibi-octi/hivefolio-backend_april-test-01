'use strict';

/**
 * follow-user-follower controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::follow-user-follower.follow-user-follower');
