'use strict';

/**
 * follow-user-follower service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::follow-user-follower.follow-user-follower');
