'use strict';

/**
 * follow-user-game controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::follow-user-game.follow-user-game');
