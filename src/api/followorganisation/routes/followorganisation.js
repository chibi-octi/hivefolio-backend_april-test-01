'use strict';

/**
 * followorganisation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::followorganisation.followorganisation');
