'use strict';

/**
 * follow-user-character service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::follow-user-character.follow-user-character');
