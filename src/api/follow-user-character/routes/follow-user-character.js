'use strict';

/**
 * follow-user-character router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::follow-user-character.follow-user-character');
