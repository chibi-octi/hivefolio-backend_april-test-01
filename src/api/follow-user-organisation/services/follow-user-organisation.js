'use strict';

/**
 * follow-user-organisation service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::follow-user-organisation.follow-user-organisation');
