'use strict';

/**
 * follow-user-organisation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::follow-user-organisation.follow-user-organisation');
